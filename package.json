{
  "name": "@yahapi/ajv-validate",
  "version": "0.2.0",
  "description": "Basic wrapper for AJV (Another JSON Schema Validator)",
  "main": "dist/index.js",
  "author": "Niels Krijger <niels@kryger.nl>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/yahapi/ajv-validate.git"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0"
  },
  "scripts": {
    "build": "rm -rf ./dist && babel src --out-dir dist",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@yahapi/errors": "^0.2.2",
    "ajv": "^4.1.5",
    "lodash": "^4.13.1",
    "moment": "^2.13.0"
  }
}
